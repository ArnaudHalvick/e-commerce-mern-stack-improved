# Phase 3 — Structured Data (JSON‑LD)

Changes

- Home: Organization and WebSite JSON‑LD with absolute URLs from env base
- Listings: BreadcrumbList from visible breadcrumb; ItemList with absolute PDP URLs
- Product: Absolute image and Offer.url; conditional AggregateRating from rating + reviews length

Verification

- In DevTools head, confirm JSON‑LD scripts render on relevant pages
- Validate copied HTML in Google Rich Results Test
