/**
 * Order History Styles
 * This file is used to import all CSS files for the Order History page
 */

// We don't need to export any JS functionality, just import the CSS files
// This import pattern ensures all CSS files are included when the application builds
import "./index.css";

// Export an empty object to satisfy the ESLint rule about default exports
const styles = {};
export default styles;
